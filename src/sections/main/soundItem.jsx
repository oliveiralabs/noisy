import styled from 'styled-components'

const SoundItemStyled = styled.div`
  background-color: white;
  box-shadow: 0px 0px 20px 0px rgba(0,0,0,0.41);
  min-height: 200px;

`

const SoundItem = () => {
  return (
    <SoundItemStyled>x</SoundItemStyled>
  )
}

export default SoundItem